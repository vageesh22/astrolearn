.venv/
__pycache__/
*.pyc
.pytest_cache/

# Geodata build artefacts. The production database is built locally from the
# GeoNames dumps (see data/DATA_SOURCES.md); only the small test fixture under
# tests/fixtures/ is committed.
data/raw/
data/work/
data/*.sqlite
data/*.sqlite.tmp
data/tz_mismatch_report.txt
.DS_Store

# Layer 10 rendered-inspection evidence (docs/LAYER10_D1_RENDERER_SPEC.md section 10.D).
# Generated by tools/render/render_inspection_set.py and tools/render/rasterise_inspection.py;
# kept locally for review. Only docs/render_inspection/INSPECTION.md, the tools and the test
# goldens under tests/fixtures/render/ are tracked.
/docs/render_inspection/svg/
/docs/render_inspection/png/
/docs/render_inspection/typography/

# Preview images the Claude desktop app saves beside the project when shown in chat.
/Claude outputs/
