"""Geodata build tooling. NOT part of the runtime package.

These modules read the GeoNames dumps in data/raw/ and build the SQLite
database the offline resolver reads. They are never imported by
``vedic_chart`` and are not installed with it.
"""
